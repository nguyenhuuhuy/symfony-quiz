{% extends 'AdminBundle::layout.html.twig' %}

{% block section %}
    <h1>QuizTagsRelations</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_quiz_tags') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('admin_quiz_tags_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
